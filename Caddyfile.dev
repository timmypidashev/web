timmypidashev.localhost {
    encode gzip

	reverse_proxy landing:3000

    @backend_routes {
		path /_event/*
		path /_upload
		path /ping
	}

	handle @backend_routes {
		reverse_proxy landing:8000
	}
}
